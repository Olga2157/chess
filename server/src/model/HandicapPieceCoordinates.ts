export default class HandicapPieceCoordinates {
    row: number;
    col: number;
    color: string

    constructor(row: number, col: number, color: string) {
        this.row = row;
        this.col = col;
        this.color = color;
    }

}
