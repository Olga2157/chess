export default class HandicapPieceCoordinates {
    row: number;
    col: number;
    color: string;
    time: string;

    constructor(row: number, col: number, color: string, time: string) {
        this.row = row;
        this.col = col;
        this.color = color;
        this.time = time;
    }

}
